pipeline {
    agent any
    environment {
        registryCredential = 'docker_credential'
        dockerImage = ''
        dockerRepository = 'ilussencio/crud_cliente'
    }
    stages {
        stage('[MAVEN] Build') {
            steps {
                withMaven() {
                    sh 'mvn clean package'
                }
            }
        }
        
        stage('[DOCKER] Build') {
            steps {
                script {
                    docker.build('${dockerRepository}:${BUILD_NUMBER}')
                }
            }
        }

        stage('[DOCKER] Push DockerHub') {
            steps {
                script {
                    dockerImage = docker.build('${dockerRepository}:${BUILD_NUMBER}')
                    docker.withRegistry('https://registry.hub.docker.com', registryCredential) {
                        dockerImage.push()
                        dockerImage.push('latest')
                    }
                }
            }
        }
        
    }
}
